/**
 * Stub file for testing module resolution, this file doesn't contain anything just
 * vitest needs a module that it can resolve for browser testing mode otherwise
 * it will error from being unable to resolve the "electron" module
 */

export {};
